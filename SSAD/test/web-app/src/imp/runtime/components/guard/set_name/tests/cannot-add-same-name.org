#+title: Test for set_name that name cannot be set if the same value is already in his name field
#+AUTHOR: VLEAD
#+DATE: [2017-07-3 Mon]
#+SETUPFILE: ../../../../../org-templates/level-5.org
#+TAGS: boilerplate(b)
#+EXCLUDE_TAGS: boilerplate
#+OPTIONS: ^:nil

* Test
  =TestSetName= object holds an instance of the =SetName= component
  against the field =component=.  This test invokes the =do= method on
  the component.

** Imports
#+BEGIN_SRC python :tangle cannot-add-same-name.py
import unittest
from runtime.objects.session.session import Session
from runtime.objects.name.name import Name
from runtime.components.guard.set_name.tests.harness import TestHarness
from runtime.datatypes.cmd.cmd import Cmd

#+END_SRC


** Test Case

   Imports the TestHarness which sets up the component.  This test creates an
   instruction with cmd =set_name= and an existing session.  The guard's do
   method succeeds and returns the instruction as result.

#+BEGIN_SRC python :tangle cannot-add-same-name.py
class TestDoPasses(TestHarness):

    def test_do_passes(self):
        print "test_do_passes"
        session = self.session
        instr = {'cmd': Cmd.set_name, 
                 'session': session, 
                 'data': {'user': self.d_user,
                          'setname': Name(val="user user")}
        }
        guard = self.component
        result = guard.do(instr)
        self.assertEqual(result, instr)

#+END_SRC

