#+title:  The =add-role= instruction type
#+DATE: [2017-06-27 Tue]
#+SETUPFILE: ../../../org-templates/level-3.org
#+TAGS: boilerplate(b)
#+EXCLUDE_TAGS: boilerplate
#+OPTIONS: ^:nil

* Implementation
#+BEGIN_SRC  python :tangle add_role_instr.py

from runtime.datatypes.cmd.cmd import Cmd
from runtime.objects.user.user import User
from runtime.objects.role.role import Role
from runtime.objects.session.session import Session
from runtime.utils.type_utils.type_utils import dict_of
from runtime.utils.type_utils.type_utils import is_equal_to

class AddRoleInstr():

    spec = {'cmd':is_equal_to(Cmd.add_role), 
            'session': Session.is_inst,
            'data': dict_of({'user' : User.is_inst,
                              'addrole': Role.is_inst})

}

    def __init__(self):
        raise Exception("can not be instantiated!")

    @staticmethod
    def is_inst(obj):
        return dict_of(AddRoleInstr.spec)(obj)

#+END_SRC

* Tests
* Package
#+BEGIN_SRC python :eval no :tangle __init__.py
print "runtime.datatypes.instr.add_role package"
#+END_SRC
